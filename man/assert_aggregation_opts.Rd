% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_aggregation_opts.R
\name{assert_aggregation_opts}
\alias{assert_aggregation_opts}
\title{Validate Aggregation Options}
\usage{
assert_aggregation_opts(aggregation, test_data = NULL, ...)
}
\arguments{
\item{aggregation}{Object to validate.}

\item{test_data}{Optional matrix to test that the aggregation functions
work correctly. If provided, both \code{ref_time} and \code{delay} functions
will be applied to the test data to verify they execute without error.}

\item{...}{Additional arguments passed to methods.}
}
\value{
NULL (invisibly) if valid, otherwise throws an error.
}
\description{
S3 generic function to validate aggregation options objects.
Uses method dispatch to perform appropriate validation based on the
aggregation class.
}
\examples{
# Validate standard observed aggregation
agg <- aggregation_observed()
assert_aggregation_opts(agg)

# Validate with test data
test_matrix <- matrix(1:12, nrow = 3, ncol = 4)
assert_aggregation_opts(agg, test_data = test_matrix)

# Validate custom aggregation
agg_custom <- aggregation_opts(
  ref_time = identity,
  delay = function(x) colSums(x, na.rm = TRUE)
)
assert_aggregation_opts(agg_custom, test_data = test_matrix)
}
\seealso{
Other aggregation_opts: 
\code{\link{aggregation_observed}()},
\code{\link{aggregation_opts}()},
\code{\link{assert_aggregation_opts.aggregation_opts}()},
\code{\link{assert_aggregation_opts.default}()},
\code{\link{print.aggregation_opts}()}
}
\concept{aggregation_opts}
