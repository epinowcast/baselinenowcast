% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_forecast_sample.R
\name{.prepare_forecast_merge}
\alias{.prepare_forecast_merge}
\title{Shared validation and merge for forecast converters}
\usage{
.prepare_forecast_merge(
  data,
  latest_obs,
  observed,
  required_output_type,
  target
)
}
\arguments{
\item{data}{A \link{baselinenowcast_df} object as returned by
\code{\link[=baselinenowcast]{baselinenowcast()}} with \code{output_type = "samples"}.}

\item{latest_obs}{A data.frame containing the latest observed counts to
score against. Must contain a \code{reference_date} column and a column with
observed counts named according to \code{observed} (default \code{"count"}).
Additional columns shared with \code{data} (such as strata columns) are used
as merge keys.}

\item{observed}{Character string giving the name of the column in
\code{latest_obs} that holds the observed value. Defaults to \code{"count"} to
match the input format of \code{\link[=baselinenowcast.data.frame]{baselinenowcast.data.frame()}}.}

\item{required_output_type}{Either \code{"samples"} or \code{"point"}.}

\item{target}{Character label for the calling function used in error
messages.}
}
\value{
A merged data.frame ready for the relevant \code{scoringutils}
converter.
}
\description{
Validates a \link{baselinenowcast_df} object, joins it with observations on
\code{reference_date} plus any shared strata columns, and warns or aborts on
duplicate keys or missing coverage. Used by
\code{\link[=as_forecast_sample.baselinenowcast_df]{as_forecast_sample.baselinenowcast_df()}} and
\code{\link[=as_forecast_point.baselinenowcast_df]{as_forecast_point.baselinenowcast_df()}}.
}
\keyword{internal}
