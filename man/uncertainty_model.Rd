% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uncertainty_model.R
\name{uncertainty_model}
\alias{uncertainty_model}
\title{Uncertainty Model Constructor}
\usage{
uncertainty_model(fit, sample, family, strategy)
}
\arguments{
\item{fit}{Function that takes observed and predicted values and returns
fitted parameters. This function should have already been wrapped by the
strategy at construction time. Signature: \verb{function(obs, pred)}.}

\item{sample}{Function that takes predictions and fitted parameters and
returns random samples. Signature: \verb{function(pred, params)}.}

\item{family}{Character string indicating the distribution family
(e.g., "nb", "poisson", "custom").}

\item{strategy}{An object of class \code{uncertainty_strategy} that was used to
wrap the base fit function.}
}
\value{
An object of class \code{c(paste0("uncertainty_", family), "uncertainty_model")} containing the fit function, sample function,
family name, and strategy.
}
\description{
Creates an uncertainty model object that defines both the probability
distribution family and the fitting strategy for uncertainty estimation.
This is the base constructor that allows users to define custom
uncertainty models.
}
\examples{
# Create a custom uncertainty model
my_model <- uncertainty_model(
  fit = function(obs, pred) mean((obs - pred)^2),
  sample = function(pred, params) rnorm(length(pred), pred, sqrt(params)),
  family = "gaussian",
  strategy = uncertainty_by_horizon()
)
print(my_model)
}
\seealso{
\code{\link[=uncertainty_nb]{uncertainty_nb()}}, \code{\link[=uncertainty_poisson]{uncertainty_poisson()}},
\code{\link[=uncertainty_strategy]{uncertainty_strategy()}}

Other uncertainty_model: 
\code{\link{assert_uncertainty_model}()},
\code{\link{assert_uncertainty_model.default}()},
\code{\link{assert_uncertainty_model.uncertainty_model}()},
\code{\link{assert_uncertainty_model.uncertainty_nb}()},
\code{\link{assert_uncertainty_model.uncertainty_poisson}()},
\code{\link{print.uncertainty_model}()}
}
\concept{uncertainty_model}
