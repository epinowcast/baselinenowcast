# Rename required columns

Rename required columns

## Usage

``` r
.rename_cols(data, old_names)
```

## Arguments

- data:

  Data.frame with the original column names

- old_names:

  The names of the columns for, in order, reference date, report date,
  and count

## Value

Data.frame with columns renamed
